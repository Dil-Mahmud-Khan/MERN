const result=Math.pow(2,3);
console.log(result);

const num1=23;
const num2=34;
const res=Math.abs(num1-num2); //for positive result
console.log(res);

const num3=3.224;
const fol=Math.round(num3);
console.log(fol);

const ceil=Math.ceil(num3);
console.log(ceil);

const floor=Math.floor(num3);
console.log(floor);

for(var i=1; i<20 ;i++){
    const random=Math.round(Math.random()*5);
    console.log(random);
}