const add = (first, second) => first + second;
const getFullName = (first, last) => first + last;
const multiply = (a, b) => a * b;

const result = multiply(8, 4);
console.log(result);

const addAll = (a, b, c, d, e) => a + b + c + d + e;

const getpie = () => 3.1416;            //no parameter arrow function

const doubleIt = (num) => num * 2;        //one parmeter arrow

const fivetimes = num => num * 5;


const doMath = (a, b, c) => {
    const firstSum = a + b;
    const secondSum = b + c;
    const multiplyResult = firstSum + secondSum;
    const kaka = multiplyResult / 4;
    return kaka;

}


